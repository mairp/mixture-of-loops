version: 2
updates:
  # The workflow pins actions by major tag; this keeps those pins current.
  # The package itself has no dependencies to track: the skill and its tests
  # are standard library only.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    labels:
      - automerge
